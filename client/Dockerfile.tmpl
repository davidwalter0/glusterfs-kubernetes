FROM {{env "GFS_SERVER_IMAGE"}}

ENV GLUSTER_MOUNTPOINT={{env "GLUSTER_MOUNTPOINT"}}
ENV GFS_VERSION={{env "GFS_VERSION"}}
ENV GFS_VERSION_MINOR={{env "GFS_VERSION_MINOR"}}
ENV GFS_VERSION={{env "GFS_VERSION"}}
ENV GFS_VERSION_PATH={{env "GFS_VERSION"}}/{{env "GFS_VERSION"}}

COPY root /

VOLUME {{env "GLUSTER_MOUNTPOINT"}}

ENTRYPOINT ["/usr/local/bin/run-init.sh"]

# local variables:
# mode: conf
# end:
